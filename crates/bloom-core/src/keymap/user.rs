/// User keymap configuration overrides.
///
/// Future: loaded from config.toml to allow rebinding keys.
pub struct UserKeymap {
    // Reserved for user-defined key overrides
}

impl Default for UserKeymap {
    fn default() -> Self {
        UserKeymap {}
    }
}