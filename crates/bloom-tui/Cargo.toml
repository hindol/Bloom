[package]
name = "bloom-tui"
version.workspace = true
edition.workspace = true

[dependencies]
bloom-core = { path = "../bloom-core" }

# TUI framework
ratatui = "0.29"
crossterm = "0.28"

# Error handling
anyhow = "1"
