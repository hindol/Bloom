[package]
name = "bloom-import"
version = "0.3.0"
edition = "2021"
license = "AGPL-3.0-or-later"

[dependencies]
bloom-core = { path = "../bloom-core" }
bloom-md = { path = "../bloom-md" }
chrono = "0.4"
tracing = "0.1"
