[package]
name = "bloom-mcp"
version = "0.4.0"
edition = "2021"
license = "AGPL-3.0-or-later"

[dependencies]
bloom-core = { path = "../bloom-core" }
bloom-md = { path = "../bloom-md" }
serde_json = "1"
chrono = "0.4"
tracing = "0.1"
